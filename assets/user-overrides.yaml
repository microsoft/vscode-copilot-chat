# yaml-language-server: $schema=../../references/TestConfig.schema.json

installExtensions:
  - id: github.copilot-chat
    mode: vsix
    path: /agent/assets/extensions/copilot-chat-0.38.0.vsix

modelSelector:
  id: gpt-5
  vendor: copilot

vscodeSettings:
  github.copilot.chat.executionSubagent.enabled: false

vscodeVersion: insiders-unreleased
