---
agent: agent
---

Implement the specified change to improve NES.

Next, run the VS Code simulator with the command `npm run simulate` and compare the new NES score against the baseline score of 69.3%